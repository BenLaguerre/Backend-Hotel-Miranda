# https://github.com/marketplace/actions/newman-action
name: Newman Run
on:
  push:
    branches:
      - Azure

jobs:
  newman:
    runs-on: ubuntu-latest
    needs: deploy
    steps:
      - uses: actions/checkout@master
      - uses: matt-ball/newman-action@master
        with:          
          collection: Restricted Backed.postman_collection.json    